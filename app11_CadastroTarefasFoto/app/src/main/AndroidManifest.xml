<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.guilherme.app11_cadastrotarefasfoto">

    <application
            android:allowBackup="true"
            android:icon="@drawable/ic_user_plus2"
            android:label="@string/app_name"
            android:roundIcon="@drawable/ic_user_plus2"
            android:supportsRtl="true"
            android:theme="@style/Theme.App10_ListaUsuariosFotoCadastro">
        <activity
        android:name=".CadastroActivity"
        android:label="Lista de Tarefas">
        </activity>

        <activity android:name="com.guilherme.app11_cadastrotarefasfoto.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>